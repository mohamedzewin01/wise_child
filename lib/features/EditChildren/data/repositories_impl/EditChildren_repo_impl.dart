import 'package:injectable/injectable.dart';
import '../../domain/repositories/EditChildren_repository.dart';

@Injectable(as: EditChildrenRepository)
class EditChildrenRepositoryImpl implements EditChildrenRepository {
  // implementation
}
