import 'package:injectable/injectable.dart';
import '../../domain/repositories/ChildMode_repository.dart';

@Injectable(as: ChildModeRepository)
class ChildModeRepositoryImpl implements ChildModeRepository {
  // implementation
}
